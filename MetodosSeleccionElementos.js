//estos metodos se usan para el document por lo tanto van acompañado del document.get....
getElementById() //selecciona un elemento por ID

getElementsByTagName() //Seleccioan todos los elementos que tengan el nombre especificado por ejemplos p(<p>) o a(<a>)
                        //devuelve una lista de elementos no un array

querySelector() // devuelve un elemento especificado con sus respectivos simbolos como en css "#" ","."  ...etc

querySelectorAll() //funciona como el anterior pero devuelve un nodelist(no es un array)